# Tasks

- Initialize repository files per GlobalCodingStandards
- Provide `sync_repo.bat` and `sync_repo.sh` for continuous sync
- Add `pr.bat` for automated commits and pushes
- Maintain `log.txt` and `book.md` for AI agents
