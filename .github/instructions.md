## 0sync repository instructions

Follow GlobalCodingStandards for project init and agent behavior.

- Keep documentation succinct
- Update `log.txt` and `book.md` for commits
- Use `pr.bat` to commit and push changes to `origin/main`
